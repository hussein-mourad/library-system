import { Login } from 'react-admin';
import BackgroundImage from '@/assets/admin-background.jpg';

function LoginPage() {
  return (
    <Login
      backgroundImage={BackgroundImage} />
  );
}
export default LoginPage;
